import { define, extend } from './define';
import { resolve, resolved } from './resolve';
import { traverse } from './traverse';

export default {
  define,
  extend,
  resolve,
  resolved,
  traverse,
};
